# Pinned dependencies for octofree Docker builds.
# Keep this in sync with the repository's top-level `requirements.txt`.
# If you prefer a single canonical file, either update the Dockerfile to
# COPY ../requirements.txt or remove this file and reference the top-level one.

pip>=24.3.1
requests>=2.32.3
python-dateutil>=2.9.0.post0
python-dotenv>=1.0.1
tweepy>=4.14.0
